spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: "jdbc:h2:mem:company;MODE=MYSQL"
    username: "sa"
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
---
spring:
  config:
    activate:
      on-profile: dev
  application:
    name: "commute-system"
  datasource:
    url: "jdbc:mysql://localhost:3306/company"
    username: "root"
    password: "AllCloud321@"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true